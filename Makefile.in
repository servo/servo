VPATH=%VPATH%

RUSTC?=rustc
RUSTFLAGS?=

RUST_SRC=$(shell find $(VPATH)/src -type f -name '*.rs')

all:    servo

servo:	src/servo.rc $(RUST_SRC)
	$(RUSTC) $(RUSTFLAGS) -o $@ $<

.PHONY:	clean

clean:
	rm -f servo

