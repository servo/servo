name: CI

on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches: [ "master", "github-actions-dev", "auto", "try", "try-linux", "try-mac" ]
  pull_request:
    branches: [ "master", "github-actions-dev" ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

env:
  RUST_BACKTRACE: 1
  SHELL: /bin/bash

jobs:
  build-win:
    name: Build (Windows)
    runs-on: windows-2019
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 2
      - name: Copy to C drive
        run: cp D:\a C:\ -Recurse
      - name: Bootstrap
        working-directory: "C:\\a\\${{ github.event.repository.name }}\\${{ github.event.repository.name }}"
        run: |
          python -m pip install --upgrade pip virtualenv
          python mach fetch
      - name: Release build
        working-directory: "C:\\a\\${{ github.event.repository.name }}\\${{ github.event.repository.name }}"
        run: python mach build --release --media-stack=dummy
      - name: Unit tests
        working-directory: "C:\\a\\${{ github.event.repository.name }}\\${{ github.event.repository.name }}"
        run: python mach test-unit --release
      - name: Smoketest
        working-directory: "C:\\a\\${{ github.event.repository.name }}\\${{ github.event.repository.name }}"
        run: python mach smoketest --angle

  build-mac:
    name: Build (macOS)
    runs-on: macos-10.15
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 2
      - name: Bootstrap
        run: |
          python3 -m pip install --upgrade pip virtualenv
          brew bundle install --verbose --no-upgrade --file=etc/taskcluster/macos/Brewfile
          brew bundle install --verbose --no-upgrade --file=etc/taskcluster/macos/Brewfile-build
          rm -rf /usr/local/etc/openssl
          rm -rf /usr/local/etc/openssl@1.1
          brew install openssl@1.1 gnu-tar
      - name: Release build
        run: |
          export OPENSSL_INCLUDE_DIR="$(brew --prefix openssl)/include"
          export OPENSSL_LIB_DIR="$(brew --prefix openssl)/lib"
          export PKG_CONFIG_PATH="$(brew --prefix libffi)/lib/pkgconfig/"
          export PKG_CONFIG_PATH="$(brew --prefix zlib)/lib/pkgconfig/:$PKG_CONFIG_PATH"
          python3 ./mach build --release
      - name: Smoketest
        run: python3 ./mach smoketest
      - name: Package binary
        run: gtar -czf target.tar.gz target/release/servo target/release/*.dylib resources
      - name: Archive binary
        uses: actions/upload-artifact@v2
        with:
          name: release-binary-macos
          path: target.tar.gz

  # mac-wpt1:
  #   #needs: build-mac
  #   runs-on: macos-10.15
  #   steps:
  #     - uses: actions/checkout@v2
  #       with:
  #         fetch-depth: 2

  #     #- name: Download release binary
  #     #  uses: actions/download-artifact@v2
  #     #  with:
  #     #    name: release-binary-macos

  #     - name: Fake build
  #       run: |
  #         wget https://joshmatthews.net/release-binary-macos.zip
  #         unzip release-binary-macos.zip

  #     - name: Prep test environment
  #       run: |
  #         brew install gnu-tar
  #         gtar -xzf target.tar.gz
  #         python3 -m pip install --upgrade pip virtualenv
  #         brew bundle install --verbose --no-upgrade --file=etc/taskcluster/macos/Brewfile
  #     - name: Smoketest
  #       run: python3 ./mach smoketest
  #     - name: Run tests
  #       run: |
  #         python3 ./mach test-wpt --release --processes=3 --timeout-multiplier=8 --total-chunks=20 --this-chunk=1 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #         python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #     - name: Archive logs
  #       uses: actions/upload-artifact@v2
  #       with:
  #         name: wpt1-logs-macos
  #         path: |
  #           test-wpt.log
  #           wpt-jsonsummary.log
  #           filtered-wpt-summary.log
  #           intermittents.log
  # mac-wpt2:
  #   #needs: build-mac
  #   runs-on: macos-10.15
  #   steps:
  #     - uses: actions/checkout@v2
  #       with:
  #         fetch-depth: 2

  #     #- name: Download release binary
  #     #  uses: actions/download-artifact@v2
  #     #  with:
  #     #    name: release-binary-macos

  #     - name: Fake build
  #       run: |
  #         wget https://joshmatthews.net/release-binary-macos.zip
  #         unzip release-binary-macos.zip

  #     - name: Prep test environment
  #       run: |
  #         brew install gnu-tar
  #         gtar -xzf target.tar.gz
  #         python3 -m pip install --upgrade pip virtualenv
  #         brew bundle install --verbose --no-upgrade --file=etc/taskcluster/macos/Brewfile
  #     - name: Smoketest
  #       run: python3 ./mach smoketest
  #     - name: Run tests
  #       run: |
  #         python3 ./mach test-wpt --release --processes=3 --timeout-multiplier=8 --total-chunks=20 --this-chunk=2 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #         python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #     - name: Archive logs
  #       uses: actions/upload-artifact@v2
  #       with:
  #         name: wpt2-logs-macos
  #         path: |
  #           test-wpt.log
  #           wpt-jsonsummary.log
  #           filtered-wpt-summary.log
  #           intermittents.log
  # mac-wpt3:
  #   #needs: build-mac
  #   runs-on: macos-10.15
  #   steps:
  #     - uses: actions/checkout@v2
  #       with:
  #         fetch-depth: 2

  #     #- name: Download release binary
  #     #  uses: actions/download-artifact@v2
  #     #  with:
  #     #    name: release-binary-macos

  #     - name: Fake build
  #       run: |
  #         wget https://joshmatthews.net/release-binary-macos.zip
  #         unzip release-binary-macos.zip

  #     - name: Prep test environment
  #       run: |
  #         brew install gnu-tar
  #         gtar -xzf target.tar.gz
  #         python3 -m pip install --upgrade pip virtualenv
  #         brew bundle install --verbose --no-upgrade --file=etc/taskcluster/macos/Brewfile
  #     - name: Smoketest
  #       run: python3 ./mach smoketest
  #     - name: Run tests
  #       run: |
  #         python3 ./mach test-wpt --release --processes=3 --timeout-multiplier=8 --total-chunks=20 --this-chunk=3 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #         python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #     - name: Archive logs
  #       uses: actions/upload-artifact@v2
  #       with:
  #         name: wpt3-logs-macos
  #         path: |
  #           test-wpt.log
  #           wpt-jsonsummary.log
  #           filtered-wpt-summary.log
  #           intermittents.log
  # mac-wpt4:
  #   #needs: build-mac
  #   runs-on: macos-10.15
  #   steps:
  #     - uses: actions/checkout@v2
  #       with:
  #         fetch-depth: 2

  #     #- name: Download release binary
  #     #  uses: actions/download-artifact@v2
  #     #  with:
  #     #    name: release-binary-macos

  #     - name: Fake build
  #       run: |
  #         wget https://joshmatthews.net/release-binary-macos.zip
  #         unzip release-binary-macos.zip

  #     - name: Prep test environment
  #       run: |
  #         brew install gnu-tar
  #         gtar -xzf target.tar.gz
  #         python3 -m pip install --upgrade pip virtualenv
  #         brew bundle install --verbose --no-upgrade --file=etc/taskcluster/macos/Brewfile
  #     - name: Smoketest
  #       run: python3 ./mach smoketest
  #     - name: Run tests
  #       run: |
  #         python3 ./mach test-wpt --release --processes=3 --timeout-multiplier=8 --total-chunks=20 --this-chunk=4 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #         python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #     - name: Archive logs
  #       uses: actions/upload-artifact@v2
  #       with:
  #         name: wpt4-logs-macos
  #         path: |
  #           test-wpt.log
  #           wpt-jsonsummary.log
  #           filtered-wpt-summary.log
  #           intermittents.log
  # mac-wpt5:
  #   #needs: build-mac
  #   runs-on: macos-10.15
  #   steps:
  #     - uses: actions/checkout@v2
  #       with:
  #         fetch-depth: 2

  #     #- name: Download release binary
  #     #  uses: actions/download-artifact@v2
  #     #  with:
  #     #    name: release-binary-macos

  #     - name: Fake build
  #       run: |
  #         wget https://joshmatthews.net/release-binary-macos.zip
  #         unzip release-binary-macos.zip

  #     - name: Prep test environment
  #       run: |
  #         brew install gnu-tar
  #         gtar -xzf target.tar.gz
  #         python3 -m pip install --upgrade pip virtualenv
  #         brew bundle install --verbose --no-upgrade --file=etc/taskcluster/macos/Brewfile
  #     - name: Smoketest
  #       run: python3 ./mach smoketest
  #     - name: Run tests
  #       run: |
  #         python3 ./mach test-wpt --release --processes=3 --timeout-multiplier=8 --total-chunks=20 --this-chunk=5 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #         python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #     - name: Archive logs
  #       uses: actions/upload-artifact@v2
  #       with:
  #         name: wpt5-logs-macos
  #         path: |
  #           test-wpt.log
  #           wpt-jsonsummary.log
  #           filtered-wpt-summary.log
  #           intermittents.log
  # mac-wpt6:
  #   #needs: build-mac
  #   runs-on: macos-10.15
  #   steps:
  #     - uses: actions/checkout@v2
  #       with:
  #         fetch-depth: 2

  #     #- name: Download release binary
  #     #  uses: actions/download-artifact@v2
  #     #  with:
  #     #    name: release-binary-macos

  #     - name: Fake build
  #       run: |
  #         wget https://joshmatthews.net/release-binary-macos.zip
  #         unzip release-binary-macos.zip

  #     - name: Prep test environment
  #       run: |
  #         brew install gnu-tar
  #         gtar -xzf target.tar.gz
  #         python3 -m pip install --upgrade pip virtualenv
  #         brew bundle install --verbose --no-upgrade --file=etc/taskcluster/macos/Brewfile
  #     - name: Smoketest
  #       run: python3 ./mach smoketest
  #     - name: Run tests
  #       run: |
  #         python3 ./mach test-wpt --release --processes=3 --timeout-multiplier=8 --total-chunks=20 --this-chunk=6 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #         python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #     - name: Archive logs
  #       uses: actions/upload-artifact@v2
  #       with:
  #         name: wpt6-logs-macos
  #         path: |
  #           test-wpt.log
  #           wpt-jsonsummary.log
  #           filtered-wpt-summary.log
  #           intermittents.log
  # mac-wpt7:
  #   #needs: build-mac
  #   runs-on: macos-10.15
  #   steps:
  #     - uses: actions/checkout@v2
  #       with:
  #         fetch-depth: 2

  #     #- name: Download release binary
  #     #  uses: actions/download-artifact@v2
  #     #  with:
  #     #    name: release-binary-macos

  #     - name: Fake build
  #       run: |
  #         wget https://joshmatthews.net/release-binary-macos.zip
  #         unzip release-binary-macos.zip

  #     - name: Prep test environment
  #       run: |
  #         brew install gnu-tar
  #         gtar -xzf target.tar.gz
  #         python3 -m pip install --upgrade pip virtualenv
  #         brew bundle install --verbose --no-upgrade --file=etc/taskcluster/macos/Brewfile
  #     - name: Smoketest
  #       run: python3 ./mach smoketest
  #     - name: Run tests
  #       run: |
  #         python3 ./mach test-wpt --release --processes=3 --timeout-multiplier=8 --total-chunks=20 --this-chunk=7 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #         python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #     - name: Archive logs
  #       uses: actions/upload-artifact@v2
  #       with:
  #         name: wpt7-logs-macos
  #         path: |
  #           test-wpt.log
  #           wpt-jsonsummary.log
  #           filtered-wpt-summary.log
  #           intermittents.log
  # mac-wpt8:
  #   #needs: build-mac
  #   runs-on: macos-10.15
  #   steps:
  #     - uses: actions/checkout@v2
  #       with:
  #         fetch-depth: 2

  #     #- name: Download release binary
  #     #  uses: actions/download-artifact@v2
  #     #  with:
  #     #    name: release-binary-macos

  #     - name: Fake build
  #       run: |
  #         wget https://joshmatthews.net/release-binary-macos.zip
  #         unzip release-binary-macos.zip

  #     - name: Prep test environment
  #       run: |
  #         brew install gnu-tar
  #         gtar -xzf target.tar.gz
  #         python3 -m pip install --upgrade pip virtualenv
  #         brew bundle install --verbose --no-upgrade --file=etc/taskcluster/macos/Brewfile
  #     - name: Smoketest
  #       run: python3 ./mach smoketest
  #     - name: Run tests
  #       run: |
  #         python3 ./mach test-wpt --release --processes=3 --timeout-multiplier=8 --total-chunks=20 --this-chunk=8 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #         python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #     - name: Archive logs
  #       uses: actions/upload-artifact@v2
  #       with:
  #         name: wpt8-logs-macos
  #         path: |
  #           test-wpt.log
  #           wpt-jsonsummary.log
  #           filtered-wpt-summary.log
  #           intermittents.log
  # mac-wpt9:
  #   #needs: build-mac
  #   runs-on: macos-10.15
  #   steps:
  #     - uses: actions/checkout@v2
  #       with:
  #         fetch-depth: 2

  #     #- name: Download release binary
  #     #  uses: actions/download-artifact@v2
  #     #  with:
  #     #    name: release-binary-macos

  #     - name: Fake build
  #       run: |
  #         wget https://joshmatthews.net/release-binary-macos.zip
  #         unzip release-binary-macos.zip

  #     - name: Prep test environment
  #       run: |
  #         brew install gnu-tar
  #         gtar -xzf target.tar.gz
  #         python3 -m pip install --upgrade pip virtualenv
  #         brew bundle install --verbose --no-upgrade --file=etc/taskcluster/macos/Brewfile
  #     - name: Smoketest
  #       run: python3 ./mach smoketest
  #     - name: Run tests
  #       run: |
  #         python3 ./mach test-wpt --release --processes=3 --timeout-multiplier=8 --total-chunks=20 --this-chunk=9 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #         python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #     - name: Archive logs
  #       uses: actions/upload-artifact@v2
  #       with:
  #         name: wpt9-logs-macos
  #         path: |
  #           test-wpt.log
  #           wpt-jsonsummary.log
  #           filtered-wpt-summary.log
  #           intermittents.log
  # mac-wpt10:
  #   #needs: build-mac
  #   runs-on: macos-10.15
  #   steps:
  #     - uses: actions/checkout@v2
  #       with:
  #         fetch-depth: 2

  #     #- name: Download release binary
  #     #  uses: actions/download-artifact@v2
  #     #  with:
  #     #    name: release-binary-macos

  #     - name: Fake build
  #       run: |
  #         wget https://joshmatthews.net/release-binary-macos.zip
  #         unzip release-binary-macos.zip

  #     - name: Prep test environment
  #       run: |
  #         brew install gnu-tar
  #         gtar -xzf target.tar.gz
  #         python3 -m pip install --upgrade pip virtualenv
  #         brew bundle install --verbose --no-upgrade --file=etc/taskcluster/macos/Brewfile
  #     - name: Smoketest
  #       run: python3 ./mach smoketest
  #     - name: Run tests
  #       run: |
  #         python3 ./mach test-wpt --release --processes=3 --timeout-multiplier=8 --total-chunks=20 --this-chunk=10 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #         python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #     - name: Archive logs
  #       uses: actions/upload-artifact@v2
  #       with:
  #         name: wpt10-logs-macos
  #         path: |
  #           test-wpt.log
  #           wpt-jsonsummary.log
  #           filtered-wpt-summary.log
  #           intermittents.log
  # mac-wpt11:
  #   #needs: build-mac
  #   runs-on: macos-10.15
  #   steps:
  #     - uses: actions/checkout@v2
  #       with:
  #         fetch-depth: 2

  #     #- name: Download release binary
  #     #  uses: actions/download-artifact@v2
  #     #  with:
  #     #    name: release-binary-macos

  #     - name: Fake build
  #       run: |
  #         wget https://joshmatthews.net/release-binary-macos.zip
  #         unzip release-binary-macos.zip

  #     - name: Prep test environment
  #       run: |
  #         brew install gnu-tar
  #         gtar -xzf target.tar.gz
  #         python3 -m pip install --upgrade pip virtualenv
  #         brew bundle install --verbose --no-upgrade --file=etc/taskcluster/macos/Brewfile
  #     - name: Smoketest
  #       run: python3 ./mach smoketest
  #     - name: Run tests
  #       run: |
  #         python3 ./mach test-wpt --release --processes=3 --timeout-multiplier=8 --total-chunks=20 --this-chunk=11 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #         python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #     - name: Archive logs
  #       uses: actions/upload-artifact@v2
  #       with:
  #         name: wpt11-logs-macos
  #         path: |
  #           test-wpt.log
  #           wpt-jsonsummary.log
  #           filtered-wpt-summary.log
  #           intermittents.log
  # mac-wpt12:
  #   #needs: build-mac
  #   runs-on: macos-10.15
  #   steps:
  #     - uses: actions/checkout@v2
  #       with:
  #         fetch-depth: 2

  #     #- name: Download release binary
  #     #  uses: actions/download-artifact@v2
  #     #  with:
  #     #    name: release-binary-macos

  #     - name: Fake build
  #       run: |
  #         wget https://joshmatthews.net/release-binary-macos.zip
  #         unzip release-binary-macos.zip

  #     - name: Prep test environment
  #       run: |
  #         brew install gnu-tar
  #         gtar -xzf target.tar.gz
  #         python3 -m pip install --upgrade pip virtualenv
  #         brew bundle install --verbose --no-upgrade --file=etc/taskcluster/macos/Brewfile
  #     - name: Smoketest
  #       run: python3 ./mach smoketest
  #     - name: Run tests
  #       run: |
  #         python3 ./mach test-wpt --release --processes=3 --timeout-multiplier=8 --total-chunks=20 --this-chunk=12 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #         python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #     - name: Archive logs
  #       uses: actions/upload-artifact@v2
  #       with:
  #         name: wpt12-logs-macos
  #         path: |
  #           test-wpt.log
  #           wpt-jsonsummary.log
  #           filtered-wpt-summary.log
  #           intermittents.log
  # mac-wpt13:
  #   #needs: build-mac
  #   runs-on: macos-10.15
  #   steps:
  #     - uses: actions/checkout@v2
  #       with:
  #         fetch-depth: 2

  #     #- name: Download release binary
  #     #  uses: actions/download-artifact@v2
  #     #  with:
  #     #    name: release-binary-macos

  #     - name: Fake build
  #       run: |
  #         wget https://joshmatthews.net/release-binary-macos.zip
  #         unzip release-binary-macos.zip

  #     - name: Prep test environment
  #       run: |
  #         brew install gnu-tar
  #         gtar -xzf target.tar.gz
  #         python3 -m pip install --upgrade pip virtualenv
  #         brew bundle install --verbose --no-upgrade --file=etc/taskcluster/macos/Brewfile
  #     - name: Smoketest
  #       run: python3 ./mach smoketest
  #     - name: Run tests
  #       run: |
  #         python3 ./mach test-wpt --release --processes=3 --timeout-multiplier=8 --total-chunks=20 --this-chunk=13 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #         python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #     - name: Archive logs
  #       uses: actions/upload-artifact@v2
  #       with:
  #         name: wpt13-logs-macos
  #         path: |
  #           test-wpt.log
  #           wpt-jsonsummary.log
  #           filtered-wpt-summary.log
  #           intermittents.log
  # mac-wpt14:
  #   #needs: build-mac
  #   runs-on: macos-10.15
  #   steps:
  #     - uses: actions/checkout@v2
  #       with:
  #         fetch-depth: 2

  #     #- name: Download release binary
  #     #  uses: actions/download-artifact@v2
  #     #  with:
  #     #    name: release-binary-macos

  #     - name: Fake build
  #       run: |
  #         wget https://joshmatthews.net/release-binary-macos.zip
  #         unzip release-binary-macos.zip

  #     - name: Prep test environment
  #       run: |
  #         brew install gnu-tar
  #         gtar -xzf target.tar.gz
  #         python3 -m pip install --upgrade pip virtualenv
  #         brew bundle install --verbose --no-upgrade --file=etc/taskcluster/macos/Brewfile
  #     - name: Smoketest
  #       run: python3 ./mach smoketest
  #     - name: Run tests
  #       run: |
  #         python3 ./mach test-wpt --release --processes=3 --timeout-multiplier=8 --total-chunks=20 --this-chunk=14 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #         python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #     - name: Archive logs
  #       uses: actions/upload-artifact@v2
  #       with:
  #         name: wpt14-logs-macos
  #         path: |
  #           test-wpt.log
  #           wpt-jsonsummary.log
  #           filtered-wpt-summary.log
  #           intermittents.log
  # mac-wpt15:
  #   #needs: build-mac
  #   runs-on: macos-10.15
  #   steps:
  #     - uses: actions/checkout@v2
  #       with:
  #         fetch-depth: 2

  #     #- name: Download release binary
  #     #  uses: actions/download-artifact@v2
  #     #  with:
  #     #    name: release-binary-macos

  #     - name: Fake build
  #       run: |
  #         wget https://joshmatthews.net/release-binary-macos.zip
  #         unzip release-binary-macos.zip

  #     - name: Prep test environment
  #       run: |
  #         brew install gnu-tar
  #         gtar -xzf target.tar.gz
  #         python3 -m pip install --upgrade pip virtualenv
  #         brew bundle install --verbose --no-upgrade --file=etc/taskcluster/macos/Brewfile
  #     - name: Smoketest
  #       run: python3 ./mach smoketest
  #     - name: Run tests
  #       run: |
  #         python3 ./mach test-wpt --release --processes=3 --timeout-multiplier=8 --total-chunks=20 --this-chunk=15 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #         python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #     - name: Archive logs
  #       uses: actions/upload-artifact@v2
  #       with:
  #         name: wpt15-logs-macos
  #         path: |
  #           test-wpt.log
  #           wpt-jsonsummary.log
  #           filtered-wpt-summary.log
  #           intermittents.log
  # mac-wpt16:
  #   #needs: build-mac
  #   runs-on: macos-10.15
  #   steps:
  #     - uses: actions/checkout@v2
  #       with:
  #         fetch-depth: 2

  #     #- name: Download release binary
  #     #  uses: actions/download-artifact@v2
  #     #  with:
  #     #    name: release-binary-macos

  #     - name: Fake build
  #       run: |
  #         wget https://joshmatthews.net/release-binary-macos.zip
  #         unzip release-binary-macos.zip

  #     - name: Prep test environment
  #       run: |
  #         brew install gnu-tar
  #         gtar -xzf target.tar.gz
  #         python3 -m pip install --upgrade pip virtualenv
  #         brew bundle install --verbose --no-upgrade --file=etc/taskcluster/macos/Brewfile
  #     - name: Smoketest
  #       run: python3 ./mach smoketest
  #     - name: Run tests
  #       run: |
  #         python3 ./mach test-wpt --release --processes=3 --timeout-multiplier=8 --total-chunks=20 --this-chunk=16 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #         python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #     - name: Archive logs
  #       uses: actions/upload-artifact@v2
  #       with:
  #         name: wpt16-logs-macos
  #         path: |
  #           test-wpt.log
  #           wpt-jsonsummary.log
  #           filtered-wpt-summary.log
  #           intermittents.log
  # mac-wpt17:
  #   #needs: build-mac
  #   runs-on: macos-10.15
  #   steps:
  #     - uses: actions/checkout@v2
  #       with:
  #         fetch-depth: 2

  #     #- name: Download release binary
  #     #  uses: actions/download-artifact@v2
  #     #  with:
  #     #    name: release-binary-macos

  #     - name: Fake build
  #       run: |
  #         wget https://joshmatthews.net/release-binary-macos.zip
  #         unzip release-binary-macos.zip

  #     - name: Prep test environment
  #       run: |
  #         brew install gnu-tar
  #         gtar -xzf target.tar.gz
  #         python3 -m pip install --upgrade pip virtualenv
  #         brew bundle install --verbose --no-upgrade --file=etc/taskcluster/macos/Brewfile
  #     - name: Smoketest
  #       run: python3 ./mach smoketest
  #     - name: Run tests
  #       run: |
  #         python3 ./mach test-wpt --release --processes=3 --timeout-multiplier=8 --total-chunks=20 --this-chunk=17 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #         python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #     - name: Archive logs
  #       uses: actions/upload-artifact@v2
  #       with:
  #         name: wpt17-logs-macos
  #         path: |
  #           test-wpt.log
  #           wpt-jsonsummary.log
  #           filtered-wpt-summary.log
  #           intermittents.log
  # mac-wpt18:
  #   #needs: build-mac
  #   runs-on: macos-10.15
  #   steps:
  #     - uses: actions/checkout@v2
  #       with:
  #         fetch-depth: 2

  #     #- name: Download release binary
  #     #  uses: actions/download-artifact@v2
  #     #  with:
  #     #    name: release-binary-macos

  #     - name: Fake build
  #       run: |
  #         wget https://joshmatthews.net/release-binary-macos.zip
  #         unzip release-binary-macos.zip

  #     - name: Prep test environment
  #       run: |
  #         brew install gnu-tar
  #         gtar -xzf target.tar.gz
  #         python3 -m pip install --upgrade pip virtualenv
  #         brew bundle install --verbose --no-upgrade --file=etc/taskcluster/macos/Brewfile
  #     - name: Smoketest
  #       run: python3 ./mach smoketest
  #     - name: Run tests
  #       run: |
  #         python3 ./mach test-wpt --release --processes=3 --timeout-multiplier=8 --total-chunks=20 --this-chunk=18 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #         python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #     - name: Archive logs
  #       uses: actions/upload-artifact@v2
  #       with:
  #         name: wpt18-logs-macos
  #         path: |
  #           test-wpt.log
  #           wpt-jsonsummary.log
  #           filtered-wpt-summary.log
  #           intermittents.log
  # mac-wpt19:
  #   #needs: build-mac
  #   runs-on: macos-10.15
  #   steps:
  #     - uses: actions/checkout@v2
  #       with:
  #         fetch-depth: 2

  #     #- name: Download release binary
  #     #  uses: actions/download-artifact@v2
  #     #  with:
  #     #    name: release-binary-macos

  #     - name: Fake build
  #       run: |
  #         wget https://joshmatthews.net/release-binary-macos.zip
  #         unzip release-binary-macos.zip

  #     - name: Prep test environment
  #       run: |
  #         brew install gnu-tar
  #         gtar -xzf target.tar.gz
  #         python3 -m pip install --upgrade pip virtualenv
  #         brew bundle install --verbose --no-upgrade --file=etc/taskcluster/macos/Brewfile
  #     - name: Smoketest
  #       run: python3 ./mach smoketest
  #     - name: Run tests
  #       run: |
  #         python3 ./mach test-wpt --release --processes=3 --timeout-multiplier=8 --total-chunks=20 --this-chunk=19 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #         python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #     - name: Archive logs
  #       uses: actions/upload-artifact@v2
  #       with:
  #         name: wpt19-logs-macos
  #         path: |
  #           test-wpt.log
  #           wpt-jsonsummary.log
  #           filtered-wpt-summary.log
  #           intermittents.log
  # mac-wpt20:
  #   #needs: build-mac
  #   runs-on: macos-10.15
  #   steps:
  #     - uses: actions/checkout@v2
  #       with:
  #         fetch-depth: 2

  #     #- name: Download release binary
  #     #  uses: actions/download-artifact@v2
  #     #  with:
  #     #    name: release-binary-macos

  #     - name: Fake build
  #       run: |
  #         wget https://joshmatthews.net/release-binary-macos.zip
  #         unzip release-binary-macos.zip

  #     - name: Prep test environment
  #       run: |
  #         brew install gnu-tar
  #         gtar -xzf target.tar.gz
  #         python3 -m pip install --upgrade pip virtualenv
  #         brew bundle install --verbose --no-upgrade --file=etc/taskcluster/macos/Brewfile
  #     - name: Smoketest
  #       run: python3 ./mach smoketest
  #     - name: Run tests
  #       run: |
  #         python3 ./mach test-wpt --release --processes=3 --timeout-multiplier=8 --total-chunks=20 --this-chunk=20 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #         python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #     - name: Archive logs
  #       uses: actions/upload-artifact@v2
  #       with:
  #         name: wpt20-logs-macos
  #         path: |
  #           test-wpt.log
  #           wpt-jsonsummary.log
  #           filtered-wpt-summary.log
  #           intermittents.log

  build-linux:
    name: Build (Linux)
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 2
      - name: Bootstrap
        run: |
          python3 -m pip install --upgrade pip virtualenv
          sudo apt update
          python3 ./mach bootstrap
      - name: Release build
        run: python3 ./mach build --release
      - name: Package binary
        run: tar -czf target.tar.gz target/release/servo resources
      - name: Archive binary
        uses: actions/upload-artifact@v2
        with:
          name: release-binary
          path: target.tar.gz

  # linux-wpt1:
  #  #needs: build-linux
  #  runs-on: ubuntu-20.04
  #  steps:
  #    - uses: actions/checkout@v2
  #      with:
  #        fetch-depth: 2

  #    #- name: Download release binary
  #    #  uses: actions/download-artifact@v2
  #    #  with:
  #    #    name: release-binary

  #    - name: Fake build
  #      run: |
  #        wget https://joshmatthews.net/release-binary.zip
  #        unzip release-binary.zip

  #    - name: Prep test environment
  #      run: |
  #        tar -xzf target.tar.gz
  #        python3 -m pip install --upgrade pip virtualenv
  #        sudo apt update
  #        sudo apt install -qy --no-install-recommends libgl1 libssl1.1 libdbus-1-3 libxcb-xfixes0-dev libxcb-shape0-dev libunwind8 libegl1-mesa
  #        wget http://mirrors.kernel.org/ubuntu/pool/main/libf/libffi/libffi6_3.2.1-8_amd64.deb
  #        sudo apt install ./libffi6_3.2.1-8_amd64.deb
  #        python3 ./mach bootstrap-gstreamer

  #    - name: Run tests
  #      run: |
  #        python3 ./mach test-wpt --release --processes=2 --total-chunks=20 --this-chunk=1 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #        python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #    - name: Archive logs
  #      uses: actions/upload-artifact@v2
  #      with:
  #        name: wpt1-logs-linux
  #        path: |
  #          test-wpt.log
  #          wpt-jsonsummary.log
  #          filtered-wpt-summary.log
  #          intermittents.log
  # linux-wpt2:
  #  #needs: build-linux
  #  runs-on: ubuntu-20.04
  #  steps:
  #    - uses: actions/checkout@v2
  #      with:
  #        fetch-depth: 2

  #    #- name: Download release binary
  #    #  uses: actions/download-artifact@v2
  #    #  with:
  #    #    name: release-binary

  #    - name: Fake build
  #      run: |
  #        wget https://joshmatthews.net/release-binary.zip
  #        unzip release-binary.zip

  #    - name: Prep test environment
  #      run: |
  #        tar -xzf target.tar.gz
  #        python3 -m pip install --upgrade pip virtualenv
  #        sudo apt update
  #        sudo apt install -qy --no-install-recommends libgl1 libssl1.1 libdbus-1-3 libxcb-xfixes0-dev libxcb-shape0-dev libunwind8 libegl1-mesa
  #        wget http://mirrors.kernel.org/ubuntu/pool/main/libf/libffi/libffi6_3.2.1-8_amd64.deb
  #        sudo apt install ./libffi6_3.2.1-8_amd64.deb
  #        python3 ./mach bootstrap-gstreamer

  #    - name: Run tests
  #      run: |
  #        python3 ./mach test-wpt --release --processes=2 --total-chunks=20 --this-chunk=2 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #        python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #    - name: Archive logs
  #      uses: actions/upload-artifact@v2
  #      with:
  #        name: wpt2-logs-linux
  #        path: |
  #          test-wpt.log
  #          wpt-jsonsummary.log
  #          filtered-wpt-summary.log
  #          intermittents.log
  # linux-wpt3:
  #  #needs: build-linux
  #  runs-on: ubuntu-20.04
  #  steps:
  #    - uses: actions/checkout@v2
  #      with:
  #        fetch-depth: 2

  #    #- name: Download release binary
  #    #  uses: actions/download-artifact@v2
  #    #  with:
  #    #    name: release-binary

  #    - name: Fake build
  #      run: |
  #        wget https://joshmatthews.net/release-binary.zip
  #        unzip release-binary.zip

  #    - name: Prep test environment
  #      run: |
  #        tar -xzf target.tar.gz
  #        python3 -m pip install --upgrade pip virtualenv
  #        sudo apt update
  #        sudo apt install -qy --no-install-recommends libgl1 libssl1.1 libdbus-1-3 libxcb-xfixes0-dev libxcb-shape0-dev libunwind8 libegl1-mesa
  #        wget http://mirrors.kernel.org/ubuntu/pool/main/libf/libffi/libffi6_3.2.1-8_amd64.deb
  #        sudo apt install ./libffi6_3.2.1-8_amd64.deb
  #        python3 ./mach bootstrap-gstreamer

  #    - name: Run tests
  #      run: |
  #        python3 ./mach test-wpt --release --processes=2 --total-chunks=20 --this-chunk=3 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #        python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #    - name: Archive logs
  #      uses: actions/upload-artifact@v2
  #      with:
  #        name: wpt3-logs-linux
  #        path: |
  #          test-wpt.log
  #          wpt-jsonsummary.log
  #          filtered-wpt-summary.log
  #          intermittents.log
  # linux-wpt4:
  #  #needs: build-linux
  #  runs-on: ubuntu-20.04
  #  steps:
  #    - uses: actions/checkout@v2
  #      with:
  #        fetch-depth: 2

  #    #- name: Download release binary
  #    #  uses: actions/download-artifact@v2
  #    #  with:
  #    #    name: release-binary

  #    - name: Fake build
  #      run: |
  #        wget https://joshmatthews.net/release-binary.zip
  #        unzip release-binary.zip

  #    - name: Prep test environment
  #      run: |
  #        tar -xzf target.tar.gz
  #        python3 -m pip install --upgrade pip virtualenv
  #        sudo apt update
  #        sudo apt install -qy --no-install-recommends libgl1 libssl1.1 libdbus-1-3 libxcb-xfixes0-dev libxcb-shape0-dev libunwind8 libegl1-mesa
  #        wget http://mirrors.kernel.org/ubuntu/pool/main/libf/libffi/libffi6_3.2.1-8_amd64.deb
  #        sudo apt install ./libffi6_3.2.1-8_amd64.deb
  #        python3 ./mach bootstrap-gstreamer

  #    - name: Run tests
  #      run: |
  #        python3 ./mach test-wpt --release --processes=2 --total-chunks=20 --this-chunk=4 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #        python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #    - name: Archive logs
  #      uses: actions/upload-artifact@v2
  #      with:
  #        name: wpt4-logs-linux
  #        path: |
  #          test-wpt.log
  #          wpt-jsonsummary.log
  #          filtered-wpt-summary.log
  #          intermittents.log
  # linux-wpt5:
  #  #needs: build-linux
  #  runs-on: ubuntu-20.04
  #  steps:
  #    - uses: actions/checkout@v2
  #      with:
  #        fetch-depth: 2

  #    #- name: Download release binary
  #    #  uses: actions/download-artifact@v2
  #    #  with:
  #    #    name: release-binary

  #    - name: Fake build
  #      run: |
  #        wget https://joshmatthews.net/release-binary.zip
  #        unzip release-binary.zip

  #    - name: Prep test environment
  #      run: |
  #        tar -xzf target.tar.gz
  #        python3 -m pip install --upgrade pip virtualenv
  #        sudo apt update
  #        sudo apt install -qy --no-install-recommends libgl1 libssl1.1 libdbus-1-3 libxcb-xfixes0-dev libxcb-shape0-dev libunwind8 libegl1-mesa
  #        wget http://mirrors.kernel.org/ubuntu/pool/main/libf/libffi/libffi6_3.2.1-8_amd64.deb
  #        sudo apt install ./libffi6_3.2.1-8_amd64.deb
  #        python3 ./mach bootstrap-gstreamer

  #    - name: Run tests
  #      run: |
  #        python3 ./mach test-wpt --release --processes=2 --total-chunks=20 --this-chunk=5 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #        python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #    - name: Archive logs
  #      uses: actions/upload-artifact@v2
  #      with:
  #        name: wpt5-logs-linux
  #        path: |
  #          test-wpt.log
  #          wpt-jsonsummary.log
  #          filtered-wpt-summary.log
  #          intermittents.log
  # linux-wpt6:
  #  #needs: build-linux
  #  runs-on: ubuntu-20.04
  #  steps:
  #    - uses: actions/checkout@v2
  #      with:
  #        fetch-depth: 2

  #    #- name: Download release binary
  #    #  uses: actions/download-artifact@v2
  #    #  with:
  #    #    name: release-binary

  #    - name: Fake build
  #      run: |
  #        wget https://joshmatthews.net/release-binary.zip
  #        unzip release-binary.zip

  #    - name: Prep test environment
  #      run: |
  #        tar -xzf target.tar.gz
  #        python3 -m pip install --upgrade pip virtualenv
  #        sudo apt update
  #        sudo apt install -qy --no-install-recommends libgl1 libssl1.1 libdbus-1-3 libxcb-xfixes0-dev libxcb-shape0-dev libunwind8 libegl1-mesa
  #        wget http://mirrors.kernel.org/ubuntu/pool/main/libf/libffi/libffi6_3.2.1-8_amd64.deb
  #        sudo apt install ./libffi6_3.2.1-8_amd64.deb
  #        python3 ./mach bootstrap-gstreamer

  #    - name: Run tests
  #      run: |
  #        python3 ./mach test-wpt --release --processes=2 --total-chunks=20 --this-chunk=6 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #        python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #    - name: Archive logs
  #      uses: actions/upload-artifact@v2
  #      with:
  #        name: wpt6-logs-linux
  #        path: |
  #          test-wpt.log
  #          wpt-jsonsummary.log
  #          filtered-wpt-summary.log
  #          intermittents.log
  # linux-wpt7:
  #  #needs: build-linux
  #  runs-on: ubuntu-20.04
  #  steps:
  #    - uses: actions/checkout@v2
  #      with:
  #        fetch-depth: 2

  #    #- name: Download release binary
  #    #  uses: actions/download-artifact@v2
  #    #  with:
  #    #    name: release-binary

  #    - name: Fake build
  #      run: |
  #        wget https://joshmatthews.net/release-binary.zip
  #        unzip release-binary.zip

  #    - name: Prep test environment
  #      run: |
  #        tar -xzf target.tar.gz
  #        python3 -m pip install --upgrade pip virtualenv
  #        sudo apt update
  #        sudo apt install -qy --no-install-recommends libgl1 libssl1.1 libdbus-1-3 libxcb-xfixes0-dev libxcb-shape0-dev libunwind8 libegl1-mesa
  #        wget http://mirrors.kernel.org/ubuntu/pool/main/libf/libffi/libffi6_3.2.1-8_amd64.deb
  #        sudo apt install ./libffi6_3.2.1-8_amd64.deb
  #        python3 ./mach bootstrap-gstreamer

  #    - name: Run tests
  #      run: |
  #        python3 ./mach test-wpt --release --processes=2 --total-chunks=20 --this-chunk=7 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #        python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #    - name: Archive logs
  #      uses: actions/upload-artifact@v2
  #      with:
  #        name: wpt7-logs-linux
  #        path: |
  #          test-wpt.log
  #          wpt-jsonsummary.log
  #          filtered-wpt-summary.log
  #          intermittents.log
  # linux-wpt8:
  #  #needs: build-linux
  #  runs-on: ubuntu-20.04
  #  steps:
  #    - uses: actions/checkout@v2
  #      with:
  #        fetch-depth: 2

  #    #- name: Download release binary
  #    #  uses: actions/download-artifact@v2
  #    #  with:
  #    #    name: release-binary

  #    - name: Fake build
  #      run: |
  #        wget https://joshmatthews.net/release-binary.zip
  #        unzip release-binary.zip

  #    - name: Prep test environment
  #      run: |
  #        tar -xzf target.tar.gz
  #        python3 -m pip install --upgrade pip virtualenv
  #        sudo apt update
  #        sudo apt install -qy --no-install-recommends libgl1 libssl1.1 libdbus-1-3 libxcb-xfixes0-dev libxcb-shape0-dev libunwind8 libegl1-mesa
  #        wget http://mirrors.kernel.org/ubuntu/pool/main/libf/libffi/libffi6_3.2.1-8_amd64.deb
  #        sudo apt install ./libffi6_3.2.1-8_amd64.deb
  #        python3 ./mach bootstrap-gstreamer

  #    - name: Run tests
  #      run: |
  #        python3 ./mach test-wpt --release --processes=2 --total-chunks=20 --this-chunk=8 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #        python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #    - name: Archive logs
  #      uses: actions/upload-artifact@v2
  #      with:
  #        name: wpt8-logs-linux
  #        path: |
  #          test-wpt.log
  #          wpt-jsonsummary.log
  #          filtered-wpt-summary.log
  #          intermittents.log
  # linux-wpt9:
  #  #needs: build-linux
  #  runs-on: ubuntu-20.04
  #  steps:
  #    - uses: actions/checkout@v2
  #      with:
  #        fetch-depth: 2

  #    #- name: Download release binary
  #    #  uses: actions/download-artifact@v2
  #    #  with:
  #    #    name: release-binary

  #    - name: Fake build
  #      run: |
  #        wget https://joshmatthews.net/release-binary.zip
  #        unzip release-binary.zip

  #    - name: Prep test environment
  #      run: |
  #        tar -xzf target.tar.gz
  #        python3 -m pip install --upgrade pip virtualenv
  #        sudo apt update
  #        sudo apt install -qy --no-install-recommends libgl1 libssl1.1 libdbus-1-3 libxcb-xfixes0-dev libxcb-shape0-dev libunwind8 libegl1-mesa
  #        wget http://mirrors.kernel.org/ubuntu/pool/main/libf/libffi/libffi6_3.2.1-8_amd64.deb
  #        sudo apt install ./libffi6_3.2.1-8_amd64.deb
  #        python3 ./mach bootstrap-gstreamer

  #    - name: Run tests
  #      run: |
  #        python3 ./mach test-wpt --release --processes=2 --total-chunks=20 --this-chunk=9 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #        python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #    - name: Archive logs
  #      uses: actions/upload-artifact@v2
  #      with:
  #        name: wpt9-logs-linux
  #        path: |
  #          test-wpt.log
  #          wpt-jsonsummary.log
  #          filtered-wpt-summary.log
  #          intermittents.log
  # linux-wpt10:
  #  #needs: build-linux
  #  runs-on: ubuntu-20.04
  #  steps:
  #    - uses: actions/checkout@v2
  #      with:
  #        fetch-depth: 2

  #    #- name: Download release binary
  #    #  uses: actions/download-artifact@v2
  #    #  with:
  #    #    name: release-binary

  #    - name: Fake build
  #      run: |
  #        wget https://joshmatthews.net/release-binary.zip
  #        unzip release-binary.zip

  #    - name: Prep test environment
  #      run: |
  #        tar -xzf target.tar.gz
  #        python3 -m pip install --upgrade pip virtualenv
  #        sudo apt update
  #        sudo apt install -qy --no-install-recommends libgl1 libssl1.1 libdbus-1-3 libxcb-xfixes0-dev libxcb-shape0-dev libunwind8 libegl1-mesa
  #        wget http://mirrors.kernel.org/ubuntu/pool/main/libf/libffi/libffi6_3.2.1-8_amd64.deb
  #        sudo apt install ./libffi6_3.2.1-8_amd64.deb
  #        python3 ./mach bootstrap-gstreamer

  #    - name: Run tests
  #      run: |
  #        python3 ./mach test-wpt --release --processes=2 --total-chunks=20 --this-chunk=10 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #        python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #    - name: Archive logs
  #      uses: actions/upload-artifact@v2
  #      with:
  #        name: wpt10-logs-linux
  #        path: |
  #          test-wpt.log
  #          wpt-jsonsummary.log
  #          filtered-wpt-summary.log
  #          intermittents.log
  # linux-wpt11:
  #  #needs: build-linux
  #  runs-on: ubuntu-20.04
  #  steps:
  #    - uses: actions/checkout@v2
  #      with:
  #        fetch-depth: 2

  #    #- name: Download release binary
  #    #  uses: actions/download-artifact@v2
  #    #  with:
  #    #    name: release-binary

  #    - name: Fake build
  #      run: |
  #        wget https://joshmatthews.net/release-binary.zip
  #        unzip release-binary.zip

  #    - name: Prep test environment
  #      run: |
  #        tar -xzf target.tar.gz
  #        python3 -m pip install --upgrade pip virtualenv
  #        sudo apt update
  #        sudo apt install -qy --no-install-recommends libgl1 libssl1.1 libdbus-1-3 libxcb-xfixes0-dev libxcb-shape0-dev libunwind8 libegl1-mesa
  #        wget http://mirrors.kernel.org/ubuntu/pool/main/libf/libffi/libffi6_3.2.1-8_amd64.deb
  #        sudo apt install ./libffi6_3.2.1-8_amd64.deb
  #        python3 ./mach bootstrap-gstreamer

  #    - name: Run tests
  #      run: |
  #        python3 ./mach test-wpt --release --processes=2 --total-chunks=20 --this-chunk=11 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #        python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #    - name: Archive logs
  #      uses: actions/upload-artifact@v2
  #      with:
  #        name: wpt11-logs-linux
  #        path: |
  #          test-wpt.log
  #          wpt-jsonsummary.log
  #          filtered-wpt-summary.log
  #          intermittents.log
  # linux-wpt12:
  #  #needs: build-linux
  #  runs-on: ubuntu-20.04
  #  steps:
  #    - uses: actions/checkout@v2
  #      with:
  #        fetch-depth: 2

  #    #- name: Download release binary
  #    #  uses: actions/download-artifact@v2
  #    #  with:
  #    #    name: release-binary

  #    - name: Fake build
  #      run: |
  #        wget https://joshmatthews.net/release-binary.zip
  #        unzip release-binary.zip

  #    - name: Prep test environment
  #      run: |
  #        tar -xzf target.tar.gz
  #        python3 -m pip install --upgrade pip virtualenv
  #        sudo apt update
  #        sudo apt install -qy --no-install-recommends libgl1 libssl1.1 libdbus-1-3 libxcb-xfixes0-dev libxcb-shape0-dev libunwind8 libegl1-mesa
  #        wget http://mirrors.kernel.org/ubuntu/pool/main/libf/libffi/libffi6_3.2.1-8_amd64.deb
  #        sudo apt install ./libffi6_3.2.1-8_amd64.deb
  #        python3 ./mach bootstrap-gstreamer

  #    - name: Run tests
  #      run: |
  #        python3 ./mach test-wpt --release --processes=2 --total-chunks=20 --this-chunk=12 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #        python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #    - name: Archive logs
  #      uses: actions/upload-artifact@v2
  #      with:
  #        name: wpt12-logs-linux
  #        path: |
  #          test-wpt.log
  #          wpt-jsonsummary.log
  #          filtered-wpt-summary.log
  #          intermittents.log
  # linux-wpt13:
  #  #needs: build-linux
  #  runs-on: ubuntu-20.04
  #  steps:
  #    - uses: actions/checkout@v2
  #      with:
  #        fetch-depth: 2

  #    #- name: Download release binary
  #    #  uses: actions/download-artifact@v2
  #    #  with:
  #    #    name: release-binary

  #    - name: Fake build
  #      run: |
  #        wget https://joshmatthews.net/release-binary.zip
  #        unzip release-binary.zip

  #    - name: Prep test environment
  #      run: |
  #        tar -xzf target.tar.gz
  #        python3 -m pip install --upgrade pip virtualenv
  #        sudo apt update
  #        sudo apt install -qy --no-install-recommends libgl1 libssl1.1 libdbus-1-3 libxcb-xfixes0-dev libxcb-shape0-dev libunwind8 libegl1-mesa
  #        wget http://mirrors.kernel.org/ubuntu/pool/main/libf/libffi/libffi6_3.2.1-8_amd64.deb
  #        sudo apt install ./libffi6_3.2.1-8_amd64.deb
  #        python3 ./mach bootstrap-gstreamer

  #    - name: Run tests
  #      run: |
  #        python3 ./mach test-wpt --release --processes=2 --total-chunks=20 --this-chunk=13 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #        python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #    - name: Archive logs
  #      uses: actions/upload-artifact@v2
  #      with:
  #        name: wpt13-logs-linux
  #        path: |
  #          test-wpt.log
  #          wpt-jsonsummary.log
  #          filtered-wpt-summary.log
  #          intermittents.log
  # linux-wpt14:
  #  #needs: build-linux
  #  runs-on: ubuntu-20.04
  #  steps:
  #    - uses: actions/checkout@v2
  #      with:
  #        fetch-depth: 2

  #    #- name: Download release binary
  #    #  uses: actions/download-artifact@v2
  #    #  with:
  #    #    name: release-binary

  #    - name: Fake build
  #      run: |
  #        wget https://joshmatthews.net/release-binary.zip
  #        unzip release-binary.zip

  #    - name: Prep test environment
  #      run: |
  #        tar -xzf target.tar.gz
  #        python3 -m pip install --upgrade pip virtualenv
  #        sudo apt update
  #        sudo apt install -qy --no-install-recommends libgl1 libssl1.1 libdbus-1-3 libxcb-xfixes0-dev libxcb-shape0-dev libunwind8 libegl1-mesa
  #        wget http://mirrors.kernel.org/ubuntu/pool/main/libf/libffi/libffi6_3.2.1-8_amd64.deb
  #        sudo apt install ./libffi6_3.2.1-8_amd64.deb
  #        python3 ./mach bootstrap-gstreamer

  #    - name: Run tests
  #      run: |
  #        python3 ./mach test-wpt --release --processes=2 --total-chunks=20 --this-chunk=14 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #        python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #    - name: Archive logs
  #      uses: actions/upload-artifact@v2
  #      with:
  #        name: wpt14-logs-linux
  #        path: |
  #          test-wpt.log
  #          wpt-jsonsummary.log
  #          filtered-wpt-summary.log
  #          intermittents.log
  # linux-wpt15:
  #  #needs: build-linux
  #  runs-on: ubuntu-20.04
  #  steps:
  #    - uses: actions/checkout@v2
  #      with:
  #        fetch-depth: 2

  #    #- name: Download release binary
  #    #  uses: actions/download-artifact@v2
  #    #  with:
  #    #    name: release-binary

  #    - name: Fake build
  #      run: |
  #        wget https://joshmatthews.net/release-binary.zip
  #        unzip release-binary.zip

  #    - name: Prep test environment
  #      run: |
  #        tar -xzf target.tar.gz
  #        python3 -m pip install --upgrade pip virtualenv
  #        sudo apt update
  #        sudo apt install -qy --no-install-recommends libgl1 libssl1.1 libdbus-1-3 libxcb-xfixes0-dev libxcb-shape0-dev libunwind8 libegl1-mesa
  #        wget http://mirrors.kernel.org/ubuntu/pool/main/libf/libffi/libffi6_3.2.1-8_amd64.deb
  #        sudo apt install ./libffi6_3.2.1-8_amd64.deb
  #        python3 ./mach bootstrap-gstreamer

  #    - name: Run tests
  #      run: |
  #        python3 ./mach test-wpt --release --processes=2 --total-chunks=20 --this-chunk=15 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #        python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #    - name: Archive logs
  #      uses: actions/upload-artifact@v2
  #      with:
  #        name: wpt15-logs-linux
  #        path: |
  #          test-wpt.log
  #          wpt-jsonsummary.log
  #          filtered-wpt-summary.log
  #          intermittents.log
  # linux-wpt16:
  #  #needs: build-linux
  #  runs-on: ubuntu-20.04
  #  steps:
  #    - uses: actions/checkout@v2
  #      with:
  #        fetch-depth: 2

  #    #- name: Download release binary
  #    #  uses: actions/download-artifact@v2
  #    #  with:
  #    #    name: release-binary

  #    - name: Fake build
  #      run: |
  #        wget https://joshmatthews.net/release-binary.zip
  #        unzip release-binary.zip

  #    - name: Prep test environment
  #      run: |
  #        tar -xzf target.tar.gz
  #        python3 -m pip install --upgrade pip virtualenv
  #        sudo apt update
  #        sudo apt install -qy --no-install-recommends libgl1 libssl1.1 libdbus-1-3 libxcb-xfixes0-dev libxcb-shape0-dev libunwind8 libegl1-mesa
  #        wget http://mirrors.kernel.org/ubuntu/pool/main/libf/libffi/libffi6_3.2.1-8_amd64.deb
  #        sudo apt install ./libffi6_3.2.1-8_amd64.deb
  #        python3 ./mach bootstrap-gstreamer

  #    - name: Run tests
  #      run: |
  #        python3 ./mach test-wpt --release --processes=2 --total-chunks=20 --this-chunk=16 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #        python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #    - name: Archive logs
  #      uses: actions/upload-artifact@v2
  #      with:
  #        name: wpt16-logs-linux
  #        path: |
  #          test-wpt.log
  #          wpt-jsonsummary.log
  #          filtered-wpt-summary.log
  #          intermittents.log
  # linux-wpt17:
  #  #needs: build-linux
  #  runs-on: ubuntu-20.04
  #  steps:
  #    - uses: actions/checkout@v2
  #      with:
  #        fetch-depth: 2

  #    #- name: Download release binary
  #    #  uses: actions/download-artifact@v2
  #    #  with:
  #    #    name: release-binary

  #    - name: Fake build
  #      run: |
  #        wget https://joshmatthews.net/release-binary.zip
  #        unzip release-binary.zip

  #    - name: Prep test environment
  #      run: |
  #        tar -xzf target.tar.gz
  #        python3 -m pip install --upgrade pip virtualenv
  #        sudo apt update
  #        sudo apt install -qy --no-install-recommends libgl1 libssl1.1 libdbus-1-3 libxcb-xfixes0-dev libxcb-shape0-dev libunwind8 libegl1-mesa
  #        wget http://mirrors.kernel.org/ubuntu/pool/main/libf/libffi/libffi6_3.2.1-8_amd64.deb
  #        sudo apt install ./libffi6_3.2.1-8_amd64.deb
  #        python3 ./mach bootstrap-gstreamer

  #    - name: Run tests
  #      run: |
  #        python3 ./mach test-wpt --release --processes=2 --total-chunks=20 --this-chunk=17 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #        python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #    - name: Archive logs
  #      uses: actions/upload-artifact@v2
  #      with:
  #        name: wpt17-logs-linux
  #        path: |
  #          test-wpt.log
  #          wpt-jsonsummary.log
  #          filtered-wpt-summary.log
  #          intermittents.log
  # linux-wpt18:
  #  #needs: build-linux
  #  runs-on: ubuntu-20.04
  #  steps:
  #    - uses: actions/checkout@v2
  #      with:
  #        fetch-depth: 2

  #    #- name: Download release binary
  #    #  uses: actions/download-artifact@v2
  #    #  with:
  #    #    name: release-binary

  #    - name: Fake build
  #      run: |
  #        wget https://joshmatthews.net/release-binary.zip
  #        unzip release-binary.zip

  #    - name: Prep test environment
  #      run: |
  #        tar -xzf target.tar.gz
  #        python3 -m pip install --upgrade pip virtualenv
  #        sudo apt update
  #        sudo apt install -qy --no-install-recommends libgl1 libssl1.1 libdbus-1-3 libxcb-xfixes0-dev libxcb-shape0-dev libunwind8 libegl1-mesa
  #        wget http://mirrors.kernel.org/ubuntu/pool/main/libf/libffi/libffi6_3.2.1-8_amd64.deb
  #        sudo apt install ./libffi6_3.2.1-8_amd64.deb
  #        python3 ./mach bootstrap-gstreamer

  #    - name: Run tests
  #      run: |
  #        python3 ./mach test-wpt --release --processes=2 --total-chunks=20 --this-chunk=18 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #        python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #    - name: Archive logs
  #      uses: actions/upload-artifact@v2
  #      with:
  #        name: wpt18-logs-linux
  #        path: |
  #          test-wpt.log
  #          wpt-jsonsummary.log
  #          filtered-wpt-summary.log
  #          intermittents.log
  # linux-wpt19:
  #  #needs: build-linux
  #  runs-on: ubuntu-20.04
  #  steps:
  #    - uses: actions/checkout@v2
  #      with:
  #        fetch-depth: 2

  #    #- name: Download release binary
  #    #  uses: actions/download-artifact@v2
  #    #  with:
  #    #    name: release-binary

  #    - name: Fake build
  #      run: |
  #        wget https://joshmatthews.net/release-binary.zip
  #        unzip release-binary.zip

  #    - name: Prep test environment
  #      run: |
  #        tar -xzf target.tar.gz
  #        python3 -m pip install --upgrade pip virtualenv
  #        sudo apt update
  #        sudo apt install -qy --no-install-recommends libgl1 libssl1.1 libdbus-1-3 libxcb-xfixes0-dev libxcb-shape0-dev libunwind8 libegl1-mesa
  #        wget http://mirrors.kernel.org/ubuntu/pool/main/libf/libffi/libffi6_3.2.1-8_amd64.deb
  #        sudo apt install ./libffi6_3.2.1-8_amd64.deb
  #        python3 ./mach bootstrap-gstreamer

  #    - name: Run tests
  #      run: |
  #        python3 ./mach test-wpt --release --processes=2 --total-chunks=20 --this-chunk=19 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #        python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #    - name: Archive logs
  #      uses: actions/upload-artifact@v2
  #      with:
  #        name: wpt19-logs-linux
  #        path: |
  #          test-wpt.log
  #          wpt-jsonsummary.log
  #          filtered-wpt-summary.log
  #          intermittents.log
  # linux-wpt20:
  #  #needs: build-linux
  #  runs-on: ubuntu-20.04
  #  steps:
  #    - uses: actions/checkout@v2
  #      with:
  #        fetch-depth: 2

  #    #- name: Download release binary
  #    #  uses: actions/download-artifact@v2
  #    #  with:
  #    #    name: release-binary

  #    - name: Fake build
  #      run: |
  #        wget https://joshmatthews.net/release-binary.zip
  #        unzip release-binary.zip

  #    - name: Prep test environment
  #      run: |
  #        tar -xzf target.tar.gz
  #        python3 -m pip install --upgrade pip virtualenv
  #        sudo apt update
  #        sudo apt install -qy --no-install-recommends libgl1 libssl1.1 libdbus-1-3 libxcb-xfixes0-dev libxcb-shape0-dev libunwind8 libegl1-mesa
  #        wget http://mirrors.kernel.org/ubuntu/pool/main/libf/libffi/libffi6_3.2.1-8_amd64.deb
  #        sudo apt install ./libffi6_3.2.1-8_amd64.deb
  #        python3 ./mach bootstrap-gstreamer

  #    - name: Run tests
  #      run: |
  #        python3 ./mach test-wpt --release --processes=2 --total-chunks=20 --this-chunk=20 --log-raw=test-wpt.log --log-servojson=wpt-jsonsummary.log --always-succeed | cat
  #        python3 ./mach filter-intermittents wpt-jsonsummary.log --log-intermittents=intermittents.log --log-filteredsummary=filtered-wpt-summary.log --tracker-api=default --reporter-api=default

  #    - name: Archive logs
  #      uses: actions/upload-artifact@v2
  #      with:
  #        name: wpt20-logs-linux
  #        path: |
  #          test-wpt.log
  #          wpt-jsonsummary.log
  #          filtered-wpt-summary.log
  #          intermittents.log

  build_result:
    name: homu build finished
    runs-on: ubuntu-latest
    needs: ["build-win", "build-mac", "build-linux"]
    steps:
      - name: Mark the job as successful
        run: exit 0
        if: success()
      - name: Mark the job as unsuccessful
        run: exit 1
        if: "!success()"
