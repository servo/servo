[workspace]

[package]
name = "filterlock"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0.194", features = ["derive"] }
toml = { version = "0.8.8", features = ["preserve_order"] }
