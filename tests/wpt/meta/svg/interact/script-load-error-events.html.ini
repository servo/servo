[script-load-error-events.html]
  expected: TIMEOUT
  ['load' and 'error' events for SVG <script>, external reference, existing]
    expected: TIMEOUT

  ['load' and 'error' events for SVG <script>, external data: URL reference, existing]
    expected: NOTRUN

  ['load' and 'error' events for SVG <script>, external reference, non-existing]
    expected: NOTRUN

  ['load' and 'error' events for SVG <script>, external reference, existing, detached]
    expected: NOTRUN

  ['load' and 'error' events for SVG <script>, external reference, non-existing, detached]
    expected: NOTRUN
