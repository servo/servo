[video-worker.tentative.html]
  # Placeholder backed OffscreenCanvas transfer now succeeds, but canvas onpaint is not implemented.
  expected: TIMEOUT
  [captureElementImage repaints video frames in worker]
    expected: TIMEOUT
