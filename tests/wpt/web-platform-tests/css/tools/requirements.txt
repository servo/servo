# Warning: The CSS build system is not tested in CI at all, so do not bump these
# dependencies without making sure that the build script still works.
Template-Python==0.1.post1
html5lib==1.1
lxml==4.9.1
mercurial==4.6.1
six==1.16.0
webencodings==0.5.1
