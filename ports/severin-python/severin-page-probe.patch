diff --git a/ports/severin-python/src/bridge_probe.rs b/ports/severin-python/src/bridge_probe.rs
new file mode 100644
index 000000000000..9f379b533d83
--- /dev/null
+++ b/ports/severin-python/src/bridge_probe.rs
@@ -0,0 +1,22 @@
+//! `SEVERIN_PROBE=1` diagnostics for page-visible bridge state.
+//!
+//! This is deliberately independent from `SEVERIN_TRACE`: a probe run emits
+//! only state transitions, so a missing bridge frame does not bury the result
+//! beneath pump-level logging.
+
+/// Opt-in environment variable for the bounded page-global probe.
+pub(crate) const ENV: &str = "SEVERIN_PROBE";
+
+pub(crate) fn enabled() -> bool {
+    match std::env::var(ENV) {
+        Ok(value) => !matches!(value.as_str(), "" | "0" | "false" | "FALSE" | "off" | "OFF"),
+        Err(_) => false,
+    }
+}
+
+pub(crate) fn emit(message: std::fmt::Arguments<'_>) {
+    if enabled() {
+        eprintln!("SEVERIN_PROBE: {message}");
+    }
+}
+
diff --git a/ports/severin-python/src/lib.rs b/ports/severin-python/src/lib.rs
index 1a603a0bd915..5c9bc41e4387 100644
--- a/ports/severin-python/src/lib.rs
+++ b/ports/severin-python/src/lib.rs
@@ -1,6 +1,7 @@
 //! Experimental in-process CPython bindings for the Severin local runtime.
 
 mod bridge_trace;
+mod bridge_probe;
 
 use std::cell::RefCell;
 use std::collections::{HashMap, VecDeque};
@@ -8,5 +8,6 @@
     bridge_transport: BridgeTransport,
     pending_evaluations: Vec<PendingEvaluation>,
     trace_bridge: bool,
+    probe_bridge: bool,
     next_evaluation_id: u64,
 }
@@ -4,4 +4,5 @@
     inbound: VecDeque<BridgeFrame>,
     pending: HashMap<u64, PendingReplyTarget>,
     active_document_id: Option<String>,
+    last_page_probe: Option<Option<String>>,
 }
@@ -2,9 +2,18 @@
         self.inbound.clear();
         self.pending.clear();
         self.active_document_id = None;
+        self.last_page_probe = None;
     }
 
     fn clear_for_close(&mut self) {
         self.clear_for_navigation();
     }
+
+    fn observe_page_probe(&mut self, page_probe: Option<String>) -> bool {
+        if self.last_page_probe == Some(page_probe.clone()) {
+            return false;
+        }
+        self.last_page_probe = Some(page_probe);
+        true
+    }
 }
@@ -1,8 +1,15 @@
 const DRAIN_SCRIPT: &str = r#"
 (() => {
+  const root = globalThis.document ? document.documentElement : null;
+  const pageProbe = root ? root.getAttribute("data-severin-probe") : null;
   if (typeof globalThis.__severinDrain !== "function") {
-    return JSON.stringify({ documentId: null, frames: [] });
+    return JSON.stringify({ documentId: null, frames: [], pageProbe });
   }
-  return JSON.stringify(globalThis.__severinDrain());
+  const drained = globalThis.__severinDrain();
+  return JSON.stringify({
+    documentId: drained.documentId,
+    frames: drained.frames,
+    pageProbe,
+  });
 })()
 "#;
@@ -4,5 +4,7 @@
             trace_bridge,
             format_args!("app:new width={width} height={height}"),
         );
+        let probe_bridge = bridge_probe::enabled();
+        bridge_probe::emit(format_args!("app:new page-global probe enabled"));
 
         let mut event_loop = EventLoop::with_user_event()
@@ -1,5 +1,6 @@
             bridge_transport: BridgeTransport::default(),
             pending_evaluations: Vec::new(),
             trace_bridge,
+            probe_bridge,
             next_evaluation_id: 0,
         })
@@ -3,4 +3,16 @@
             self.bridge_transport.clear_for_navigation();
             self.bridge_transport.active_document_id = Some(document_id.clone());
         }
+        if self.probe_bridge {
+            let page_probe = drained
+                .get("pageProbe")
+                .and_then(|value| value.as_str())
+                .map(str::to_owned);
+            if self.bridge_transport.observe_page_probe(page_probe.clone()) {
+                bridge_probe::emit(format_args!(
+                    "page-global={}",
+                    page_probe.as_deref().unwrap_or("<absent>")
+                ));
+            }
+        }
         let Some(frames) = drained.get("frames").and_then(|value| value.as_array()) else {
             let message = "Severin bridge drain result did not contain a frames array".to_owned();
             self.trace(format_args!("drain: failed error={message}"));
