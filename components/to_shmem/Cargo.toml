[package]
name = "to_shmem"
version = "0.0.1"
authors = ["The Servo Project Developers"]
license = "MPL-2.0"
publish = false

[lib]
name = "to_shmem"
path = "lib.rs"

[features]
servo = ["serde", "cssparser/serde"]
gecko = []

[dependencies]
cssparser = "0.25"
serde = {version = "1.0", optional = true}
