[package]
name = "webvr_traits"
version = "0.0.1"
authors = ["The Servo Project Developers"]
license = "MPL-2.0"
publish = false

[lib]
name = "webvr_traits"
path = "lib.rs"

[dependencies]
ipc-channel = "0.8"
msg = {path = "../msg"}
rust-webvr = {version = "0.5", features = ["serde-serialization"]}
serde = "1.0"
