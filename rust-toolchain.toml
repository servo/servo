[toolchain]
# Be sure to update shell.nix and support/crown/rust-toolchain.toml when bumping this!
channel = "1.83.0"

components = [
    # For support/crown
    "llvm-tools",
    # For support/crown
    "rustc-dev",
    # For rust-analyzer
    "rust-src",
]
